var chai   = require('chai'),
    assert = require('assert');

describe('User', function () {
  describe('#save', function () {
    it('creates a user if id is null');
    it('updates a user if id is not null');
  });
});
